const getMicrosoftOauthTokens = async ({ code }) => {
    // const url = 'https://login.microsoftonline.com/common/',
    const url = 'https://login.microsoftonline.com/common/oauth2/v2.0/authorize';
    const values = {}
}

module.exports = { getMicrosoftOauthTokens }
